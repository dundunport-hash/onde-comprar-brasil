# Stack

Não assuma versões/dependências. Antes de alterar código, confirme no `package.json` e configs.

Baseline esperado do clone: Next.js/React/TypeScript/Tailwind e possivelmente Prisma, auth, upload e pagamento.

Se `.ia` divergir do código, o código vence. Não trocar PostgreSQL/MongoDB, auth, gateway, storage ou libs sem pedido explícito.
